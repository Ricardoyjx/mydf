# P0 — 正确性闭环（建议先做）

运行生命周期闭环。目前 RunRecord.status 永远停在 pending，start_run 没有把记录注册进 RunManager，所以 on_disconnect=cancel 时 run_mgr.cancel() 找不到记录，断线取消实际不生效。建议：start_run 改用 run_mgr.create() 注册记录，worker 结束后通过 task done-callback 更新 success/error/interrupted，see_consumer 的 finally 语义才能真正工作。相关文件：services.py、runs/manager.py、runs/worker.py。

自定义 Agent 体系（最有产品价值的空缺）。build_run_config 已经预留了 assistant_id → agent_name 注入，注释声称会加载 agents/ name /SOUL.md 和 per-agent 配置，但 _make_lead_agent 里 agent_name = "lead_agent" 是硬编码的，system_prompt=""、tools 为空。建议实现：按 agent_name 从 Paths.user_agent_dir() 加载 SOUL.md + config.yaml（模型、工具列表），支持注册自定义工具，/api/runs/stream 传不同的 assistant_id 就能切换人格和能力。

仓库卫生 + 文档基建：git rm --cached 运行时数据；补 .env.example（键名 + 英文注释，.env 里的中文注释也应清理，AGENTS.md 明确禁止）；README 目前是空的，建议至少写清启动步骤、环境变量表、API 一览。

## P1 — 核心功能补全

运行管理 API。RunManager/RunStore/RunRecord 已经实现了状态、并发策略、token 聚合，但没有任何路由暴露它们。建议加 /api/runs（列表、按线程查、取消、详情），前端就能展示运行状态和用量。

线程管理 API。目前只有 GET /api/threads/{id}/messages，缺线程列表、删除（Paths.delete_thread_dir 已就绪）、重命名、历史分页（last_event_id 重连在 stream bridge 里也标注了"当前忽略历史"）。

Store 持久化落地。store/async_provider.py 里 SQLite/Postgres 的 Store 实现被整体注释、统一回退 InMemoryStore。若想支撑"线程数据/跨会话状态"，应把注释掉的 AsyncSqliteStore/AsyncPostgresStore 分支启用，与 checkpointer 共用同一 connection_string（代码骨架已写好）。

多用户隔离。user_context.py 的_current_user ContextVar 从未被 set，所有请求都是 default 用户，Milvus 集合、记忆文件、agent 目录的多租户隔离全部形同虚设。接入一个最简单的认证中间件（如 X-User-Id 头或 JWT）就能让整条用户隔离链路生效。

### P2 — 扩展与工程化

RAG 前端管理界面：/api/rag/* 已完整（入库、上传、搜索、删除、分块），但 frontend/index.html 只有对话框，没有知识库管理 UI；文档上传/列表/删除需要一个页面。
记忆管理闭环：GET /api/{thread_id}/memory 的 handler 不接收 thread_id，前端也没有记忆展示；建议把记忆 API 接入用户隔离并加前端面板。
Redis StreamBridge：StreamBridgeConfig 和 NotImplementedError 已预留 Phase 2 位置，多进程部署时才需要，可以往后放。
RunEventStore：接口（runtime/events/store/base.py）已定义但无实现，运行日志/回放依赖它。
工程化：hybrid_search() 空实现、agents/factory.py 的 create_mydf_agent 死代码、_build_middlewares(custom_middlewares) 未用参数、deps.py 遗留的 Checkpointer 别名——清理掉；再加 pyright 类型检查 + Black 格式化进 CI（uv 工作流已就绪）。
我的建议是优先 P0 的三项：运行生命周期闭环是当前真实存在的行为缺陷（断线取消不生效、状态不更新），自定义 Agent 是项目差异化价值所在，仓库卫生半小时内可完成且避免后续污染。
