# AGENTS.md - Python 项目通用开发规范

你是一名精通 Python 工程化的资深开发者与 AI 协作专家。在为本项目生成代码、修改逻辑或审查代码时，必须严格遵守以下规范。

## 1. 环境与依赖管理 (uv)

- **工具链锁定**：统一使用 `uv` 作为唯一包管理器与环境管理工具。严禁使用 `pip`、`conda`、`poetry` 等其他工具。
- **依赖声明**：所有运行时与开发依赖必须声明于 `pyproject.toml`，禁止手动维护 `requirements.txt`。
- **环境同步**：安装/更新依赖统一使用 `uv sync`；安装本地可编辑包使用 `uv pip install -e .`。
- **缓存维护**：遇到依赖解析异常或磁盘空间不足时，优先执行 `uv cache prune` 清理孤立缓存，禁止手动删除缓存目录。
- **镜像配置**：国内网络环境下，优先通过 `uv` 全局配置或项目级 `pyproject.toml` 设置 PyPI 镜像源，避免每次安装时手动指定。

## 2. 项目结构与配置

- **标准布局**：核心业务代码置于 `src/<package_name>/`，测试代码置于 `tests/`，配置文件置于项目根目录。
- **敏感信息管理**：API Key、数据库凭证等敏感信息必须存放于 `.env` 文件，严禁硬编码。`.env` 必须加入 `.gitignore`。
- **配置加载**：使用 `pydantic-settings` 或 `python-dotenv` 统一管理环境变量，确保配置类型安全且与业务代码解耦。
- **入口规范**：提供清晰的 CLI 入口或启动脚本，避免散落的临时运行文件。

## 3. 中文开发者本地化适配

- **交互语言**：所有回复、解释、日志输出均使用中文。
- **注释规范**：代码注释、文档字符串（docstring）统一使用中文编写。
- **编码安全**：`.env` 文件中**严禁包含任何中文注释或非 ASCII 字符**，防止跨平台加载时出现编码错误。
- **术语一致性**：技术术语保留英文原文（如 TypedDict、Pydantic），不进行生硬翻译。

## 4. 代码质量与安全

- **代码生成原则**：生成简洁、可读的代码，除注释外不添加多余符号或装饰性内容。
- **修改审批机制**：**禁止直接修改项目文件**。所有代码变更必须以 inline diff 格式呈现，等待用户明确确认后方可应用。
- **格式化强制**：所有新生成或修改的代码必须符合 Black 格式化规范。
- **类型安全**：所有函数签名必须包含完整类型注解；复杂数据结构优先使用 `TypedDict` 或 `Pydantic BaseModel` 定义。
- **防御性编程**：关键路径必须包含 `try-except` 异常捕获，并配合结构化日志记录上下文信息，禁止静默吞掉异常。
- **安全红线**：
  - 禁止硬编码任何敏感凭证。
  - 禁止将 `.env`、`__pycache__`、`.venv`、`*.pyc` 等文件提交至版本控制。
  - 禁止在生产代码中使用 `print()` 调试，统一使用 `logging` 模块。

## 5. AI 协作专属约束

- **最小变更原则**：仅修改与当前任务直接相关的代码，避免无关重构。
- **上下文感知**：修改代码前必须先阅读相关文件，理解现有架构与设计意图，禁止凭空生成脱离项目上下文的代码。
- **可验证性**：生成的代码应附带简要的验证思路或测试建议，便于用户快速确认正确性。
- **不确定性声明**：当对项目结构、依赖版本或业务逻辑存在疑问时，必须主动提问澄清，禁止猜测性实现。

## 6. 核心开发准则

- **State 定义**：
  - 必须使用 `TypedDict` 定义 State。
  - 聊天记录等需要累加的字段，必须使用 `Annotated[list, add_messages]` 注解，确保多轮对话上下文不丢失。
- **Node 开发**：
  - 节点函数接收 `State` 并返回更新后的 `State`（字典）。
  - 保持节点逻辑单一，复杂业务逻辑应抽离为独立的工具（Tools）或服务类。
- **条件路由 (Conditional Edges)**：
  - 使用 `add_conditional_edges` 实现条件判断。
  - **强制要求**：路由函数（Router Function）必须包含 `else` 兜底逻辑，防止未知状态导致程序崩溃。
- **持久化 (Checkpointer)**：
  - 编译图时必须传入 `checkpointer=MemorySaver()`（或数据库持久化）。
  - 调用图时必须通过 `config={"configurable": {"thread_id": "xxx"}}` 传递线程 ID，以支持多会话记忆隔离。
