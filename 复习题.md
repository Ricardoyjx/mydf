# my-df 项目复习题

## 一、填空题

1. 项目后端的工作目录是 `________`，其包管理器统一使用 `________`（禁止 pip/conda）。

2. FastAPI 应用的入口文件是 `________`，网关监听端口默认为 `________`。

3. `load_dotenv()` 在 `________` 的 `________` 函数中被调用，用于加载 `________` 文件中的环境变量。

4. `InMemoryStreamBridge` 底层使用 `________` 作为事件管道，每个 `run_id` 对应一个独立的该对象。

5. SSE 消费者通过 `queue.get()` 读取事件，当 15 秒无事件时会 yield 一个 `________` 哨兵，前端收到 `________` 注释行。

6. Agent 运行结束后，`finally` 块必须调用 `________`，否则前端会一直等待不关闭连接。

7. 运行测试的命令是 `________`，测试文件位于 `________` 目录。

8. 中间件 `TodoMiddleware` 仅在 `________` 为 `True` 时才被注册到中间件链中。

## 二、单选题

9. 下列哪个环境变量用于配置 LLM 的 API Key？
   - A. `MYDF_API_KEY`
   - B. `MYDF_LLM_API_KEY`
   - C. `OPENAI_API_KEY`
   - D. `LLM_KEY`

10. `get_app_config()` 返回的 `AppConfig` 中，`models` 字段默认值是什么？
    - A. 一个包含 `deepseek-v4-flash` 的列表
    - B. 空列表 `[]`
    - C. 从 `config.yaml` 解析的模型列表
    - D. `None`

11. `run_agent_mini` 的 `graph_input` 参数为 `None` 时，会发生什么？
    - A. `astream(None)` 会抛出异常
    - B. 函数发布一个 `error` 事件并立即返回
    - C. 函数直接返回，不发布任何事件
    - D. 自动填充空字典 `{}` 继续执行

12. 关于 `asyncio.Queue` 的描述，哪一项是正确的？
    - A. 它是线程安全的，`put` 和 `get` 会阻塞操作系统线程
    - B. 它是协程安全的，`put` 和 `get` 只挂起当前协程，不阻塞线程
    - C. 它在队列满时会自动丢弃旧事件
    - D. 它只能用于单生产者单消费者场景

## 三、多选题

13. 下列哪些文件在 "阶段一" 的修复中被修改过？
    - [ ] `backend/pyproject.toml`
    - [ ] `backend/app/gateway/app.py`
    - [ ] `backend/packages/harness/my_df/agents/config/app_config.py`
    - [ ] `backend/packages/harness/my_df/agents/lead_agent/agent.py`
    - [ ] `backend/packages/harness/my_df/agents/middlewares/runtime_middlewares.py`
    - [ ] `backend/packages/harness/my_df/runtime/runs/worker.py`

14. `TodoMiddleware` 的双重注册问题是指什么？
    - [ ] `runtime_middlewares.py` 无条件创建了一个 `TodoMiddleware`
    - [ ] `_build_middlewares()` 又条件创建了另一个 `TodoMiddleware`
    - [ ] `is_plan_mode=True` 时中间件链中有两个 `TodoMiddleware` 实例
    - [ ] 两个 `TodoMiddleware` 互相覆盖导致状态丢失
    - [ ] 修复方案是让 `build_lead_runtime_middlewares()` 返回空列表

15. 关于 `StreamingResponse` 与 `asyncio.Task` 的协作，正确的说法是？
    - [ ] `run_agent_mini` 运行在 `asyncio.Task` 中，与请求协程并发执行
    - [ ] `see_consumer` 通过 `queue.get()` 阻塞等待，不空转 CPU
    - [ ] 生产者和消费者在同一个协程中串行执行
    - [ ] `publish_end` 发送的 `END_SENTINEL` 让 `subscribe` 的迭代终止
    - [ ] 心跳机制通过 `wait_for(..., timeout=15.0)` 实现

16. 中间件链的顺序（从先执行到后执行）正确的是？
    - [ ] 运行时基础中间件 → DynamicContextMiddleware → TodoMiddleware
    - [ ] TodoMiddleware → DynamicContextMiddleware → 运行时基础中间件
    - [ ] DynamicContextMiddleware → TodoMiddleware → 运行时基础中间件
    - [ ] 运行时基础中间件 → TodoMiddleware → DynamicContextMiddleware

## 四、连线题

17. 将以下组件与其职责配对：

| 组件 | 职责 |
|------|------|
| ① `InMemoryStreamBridge` | A. 管理 run 注册表，支持取消运行 |
| ② `RunManager` | B. 将 model+tool+middleware 组装为 CompiledStateGraph |
| ③ `make_lead_agent` | C. 基于 asyncio.Queue 的生产者-消费者管道 |
| ④ `see_consumer` | D. 从 bridge 读取事件并格式化为 SSE 字符串 |
| ⑤ `RunContext` | E. 持有 checkpointer/store 等基础设施依赖 |

## 五、简答题

18. 简述从 `POST /api/runs/stream` 到客户端收到 SSE 事件的完整数据流（涉及哪些组件、队列如何工作）。

19. 为什么 `_make_lead_agent()` 不再需要硬编码 `ModelConfig`？`get_app_config()` 是如何从环境变量构造模型配置的？

20. 解释 `MYDF_IS_PLAN_MODE` 环境变量的作用，以及它如何控制 `TodoMiddleware` 的注册。

---

**答案**

1. `backend/`，`uv`
2. `app/gateway/app.py`，8001
3. `app.py`，`lifespan`，`.env`
4. `asyncio.Queue`
5. `HEARTBEAT_SENTINEL`（或 `StreamEvent(id="", event="__heartbeat__", data=None)`），`: heartbeat`
6. `bridge.publish_end(run_id)`
7. `.venv/bin/python -m pytest tests/ -v`，`tests/`
8. `is_plan_mode`
9. B
10. B
11. B
12. B
13. 全选（6 个文件都被修改）
14. 前三个（A、B、C）和最后一个（E）
15. 前两个（A、B）和最后两个（D、E）
16. A
17. ①-C, ②-A, ③-B, ④-D, ⑤-E
