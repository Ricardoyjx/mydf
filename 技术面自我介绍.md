最近参与开发的核心功能和模块：

多agent编排设计：通过 Supervisor 主agent进行意图识别和任务派发，通过路由图的形式交给子代理执行，再交由质量评估的agent进行评估，有效的解决单agent在长时运行情况下状态迷失、输出质量不可控和难以观测和恢复问题。

流式交互与持久化：会话通过 SSE 流式返回，上下文context保持用户会话 ID，确保多租户隔离；使用 checkpointer 支持 memory / SQLite / Postgres 三种持久化，基于 thread_id 实现多会话记忆隔离。

FASTAPI框架开发：lifespan的搭建：预加载子代理图，预加载embedding模型和rerank模型；RESTful API开发。